import React from "react";

const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search you plant..."
      className="px-4 py-2 border-solid border-5 rounded-md shadow-sm"
    />
  );
};

export default SearchBar;
